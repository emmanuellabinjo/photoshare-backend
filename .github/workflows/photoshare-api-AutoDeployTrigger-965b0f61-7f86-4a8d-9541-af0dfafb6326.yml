name: Trigger auto deployment for photoshare-api

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/photoshare-api-AutoDeployTrigger-965b0f61-7f86-4a8d-9541-af0dfafb6326.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.PHOTOSHAREAPI_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.PHOTOSHAREAPI_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.PHOTOSHAREAPI_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.PHOTOSHAREAPI_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.PHOTOSHAREAPI_REGISTRY_PASSWORD }}
          containerAppName: photoshare-api
          resourceGroup: photoshare-rg
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


